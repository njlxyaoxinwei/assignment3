<% provide(:title, 'All Users') %>
<h1>Users List</h1>

<ul>
<% @users.each do |user| %>
  <li><%= link_to user.name, user_path(user) %>
  	<%= link_to 'Update', edit_user_path(user) %></li>
<% end %>
</ul>